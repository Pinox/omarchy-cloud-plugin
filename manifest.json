{
  "schemaVersion": 1,
  "id": "furmware.cloud",
  "name": "Cloud",
  "version": "0.2.3",
  "author": "furmware",
  "license": "MIT",
  "description": "Mount Google Drive, Dropbox, OneDrive and other cloud storage into your file manager with rclone.",
  "kinds": [
    "service",
    "bar-widget"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Cloud",
    "description": "Connect cloud storage and mount it as ordinary folders, with mount status in the bar.",
    "category": "Files",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "mountRoot": "~/Cloud",
      "vfsCacheMode": "full",
      "cacheMaxSizeGB": 8,
      "refreshIntervalSec": 15,
      "quotaIntervalMin": 20,
      "showLabel": false
    },
    "schema": [
      {
        "key": "mountRoot",
        "type": "string",
        "label": "Mount folder",
        "defaultValue": "~/Cloud",
        "description": "Where each connected service appears. One folder per service is created underneath. Changing this after connecting a service requires reconnecting it."
      },
      {
        "key": "vfsCacheMode",
        "type": "enum",
        "label": "File cache",
        "options": ["off", "minimal", "writes", "full"],
        "defaultValue": "full",
        "description": "How much rclone caches on disk. 'full' is required for editing files in place from most applications and lets recently opened files work offline. 'off' will break saving in many editors."
      },
      {
        "key": "cacheMaxSizeGB",
        "type": "integer",
        "label": "Cache size limit (GB)",
        "min": 1,
        "max": 512,
        "step": 1,
        "defaultValue": 8,
        "description": "Upper bound on disk used by the file cache. Least recently used files are evicted first."
      },
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Status refresh (seconds)",
        "min": 5,
        "max": 300,
        "step": 5,
        "defaultValue": 15,
        "description": "How often mount status is checked. This reads local system state only, so it is cheap."
      },
      {
        "key": "quotaIntervalMin",
        "type": "integer",
        "label": "Storage usage refresh (minutes)",
        "min": 5,
        "max": 240,
        "step": 5,
        "defaultValue": 20,
        "description": "How often used and total storage is fetched. This one talks to the provider over the network, so keep it slow."
      },
      {
        "key": "showLabel",
        "type": "boolean",
        "label": "Show text in bar",
        "defaultValue": false,
        "description": "Print how many services are mounted next to the bar icon."
      }
    ]
  }
}
